# Synthesize research from live evidence

The research Provider will retrieve current Research Evidence from a search API and ask DeepSeek to synthesize it into the paid result, rather than presenting model memory as web research. This keeps the Service aligned with its `web-research` Offer and makes the Execution's source material inspectable.
