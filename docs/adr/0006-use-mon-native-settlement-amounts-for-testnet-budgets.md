# Use MON-native Settlement Amounts for testnet Budgets

The first remote Provider will price Offers and enforce Task Budgets in MON, the native Monad testnet currency. Any fiat display is a Reference Price only, because a manually paired USD value cannot authoritatively constrain an on-chain Payment without a trusted exchange-rate source.
